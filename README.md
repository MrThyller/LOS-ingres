### Script pra compilar (Versão base/sem mods)
```
curl -fsSL https://raw.githubusercontent.com/MrThyller/LOSMG-ingres/refs/heads/main/LineageOS-23.2/base.sh | bash
```


> Observações: Não é necessário criar uma pasta para executar o script, pois o mesmo já faz isso automaticamente. Apenas execute-o em qualquer local e ele criará a pasta `LineageOS-MicroG` na `${HOME}` automaticamente para dar início ao trabalho de compilação automatizada.

---
